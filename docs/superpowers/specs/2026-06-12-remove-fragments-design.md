# Design: Remove Login and AI Assistant Fragments

Remove `loginFragment` and `aiAssistantFragment` from `nav_graph.xml` as they are no longer needed.

## Plan
- Modify `nav_graph.xml` to remove the specified fragments.
- Validate graph structure (manual inspection to ensure no dangling actions).
